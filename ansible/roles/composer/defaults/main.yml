---
composer_bin: /usr/local/bin/composer
composer_installer_url: https://getcomposer.org/installer
# Pin to a known-good installer SHA-384 to verify integrity. Refresh from
# https://composer.github.io/pubkeys.html / the official installer checksum.
# Leave empty to skip verification (not recommended).
composer_installer_checksum: ""

wpcli_bin: /usr/local/bin/wp
wpcli_phar_url: https://raw.githubusercontent.com/wp-cli/builds/gh-pages/phar/wp-cli.phar
