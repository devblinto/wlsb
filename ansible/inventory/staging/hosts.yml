---
# Staging inventory. Mirrors production so releases are validated identically.
all:
  children:
    web:
      hosts:
        wlsb-staging:
          ansible_host: 203.0.113.20        # ← staging server IP or DNS name
          ansible_user: root
          ansible_port: 22
