---
# Staging inventory. Mirrors production so releases are validated identically.
all:
  children:
    web:
      hosts:
        wlsb-staging:
          ansible_host: 84.247.149.242      # ← staging server IP or DNS name
          ansible_user: root
          ansible_port: 22
